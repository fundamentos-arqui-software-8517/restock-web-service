package com.uitopic.restock.platform.communications.domain.model.valueobjects;

/**
 * Enum representing the client platforms for which push notifications can be sent.
 * This enum is used to specify the platform of the client device when creating a push subscription.
 */
public enum ClientPlatform {
    WEB,
    ANDROID,
    IOS
}
