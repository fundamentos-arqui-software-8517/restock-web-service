package com.uitopic.restock.platform.communications.domain.model.valueobjects;

/**
 * Enum representing the supported notification providers for push notifications.
 * This enum is used to specify which provider is being used for a given push subscription.
 */
public enum NotificationProvider {
    FCM,
    ONE_SIGNAL,
}
